<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/tableFragment">

    <fragment
        android:id="@+id/tableFragment"
        android:name="texnopos.uz.myproject1.ui.table.TableFragment"
        android:label="TableFragment"
        tools:layout="@layout/fragment_table">
        <action
            android:id="@+id/action_tableFragment_to_bookingFragment"
            app:destination="@id/bookingFragment" />
    </fragment>
    <fragment
        android:id="@+id/bookingFragment"
        android:name="texnopos.uz.myproject1.ui.booking.BookingFragment"
        android:label="" >
        <argumentBookingFragment
            android:name="tableName"
            android:defaultValue='""' />
        <argument
            android:name="tableName"
            app:argType="string"
            android:defaultValue='""' />
    </fragment>

</navigation>